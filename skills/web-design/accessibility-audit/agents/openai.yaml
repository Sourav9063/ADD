interface:
  display_name: "Accessibility Audit"
  short_description: "Audit an interface against WCAG"
  default_prompt: "Use $accessibility-audit to check this interface for keyboard, contrast, focus, and ARIA issues."
policy:
  allow_implicit_invocation: true
