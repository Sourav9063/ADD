interface:
  display_name: "Alert and Banner"
  short_description: "Design persistent messages"
  default_prompt: "Use $alert-banner to build or review this banner, its severity, and its dismissal."
policy:
  allow_implicit_invocation: true
