interface:
  display_name: "Create Generic Next.js Component"
  short_description: "Create Next.js components"
  default_prompt: "Use $create-component-agnostic to create or update a Next.js component, client component, provider, form, table, filter, or reusable UI."
policy:
  allow_implicit_invocation: false
