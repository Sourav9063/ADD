interface:
  display_name: "Unslop"
  short_description: "Strip AI tells from prose"
  default_prompt: "Use $unslop to remove AI patterns from this writing and give it human voice."
policy:
  allow_implicit_invocation: true
